/*
    Operadores Lógicos
    && -> AND -> E -> Todas as expresso~es precisam ser verdadeiras.
    || -> OR -> OU
    ! -> NOT -> NÂO
*/

const expressaoAnd = true && true;
const expressaoOr = true || false;